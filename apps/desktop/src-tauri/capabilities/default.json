{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capabilities for the main window. App-defined commands are permitted by default; no extra plugin permissions needed for the offline MVP.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
